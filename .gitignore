# Dependencies
node_modules/

# Build output
dist/
build/

# Environment / secrets
.env
.env.local
*.local

# Server-specific compose (Traefik, domains, secrets) — not committed
docker-compose.server.yml

# Logs
*.log
npm-debug.log*

# OS / editor / tooling
.DS_Store
Thumbs.db
.idea/
.vscode/
.claude/

# Docker volume data (if ever bind-mounted locally)
pgdata/
